[package]
name = "gw-wire"
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
license = "GPL-3.0-only"
description = "Explicit little-endian wire codecs for Glasswyrm"

[lib]
path = "src/lib.rs"

[dependencies]
gw-types = { path = "../gw-types" }
