# Architecture Notes

This directory is for architecture notes that expand on
`docs/GLASSWYRM_SPEC.md`.

The first implementation phase should keep the core server, protocol layer,
headless compositor, and software renderer independently testable.
